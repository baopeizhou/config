server:
  port: 2222
eureka:
  instance: 
     hostname: localhost
     environment: dev
  client: 
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://localhost/registry/